//write a c program to access the array elements using pointers.
#include<stdio.h>
int main(){
	int n;
	int data[5];
	printf("enter elements:");
	for(int i=0;i<5;++i){
		scanf("%d",data+i);
	}
	printf("The elements are:");
	for(int i=0;i<5;++i){
		printf("%d",*(data+i));
	}
	return 0;
}
